apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: falco
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/meDracula/FlatlineOps.git
    targetRevision: main
    path: apps/detection/falco
    helm:
      valueFiles:
        - values.yaml
  destination:
    namespace: falco
    server: https://kubernetes.default.svc
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      enabled: false
