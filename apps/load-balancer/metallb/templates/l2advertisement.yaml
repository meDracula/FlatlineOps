apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metdata:
  name: lab-l2-advert
  namespace: {{ .Release.Namespace }}
spec:
  ipAddressPools:
    - ingress-pool
  nodeSelectors:
    - matchLabels:
        network-role: ingress
