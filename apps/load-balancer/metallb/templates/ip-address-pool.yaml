apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: ingress-pool
  namespace: {{ .Release.Namespace }}
spec:
  {{- with .Values.ingress.ipAddressPool }}
  addresses:
    {{- toYaml . | nindent 4 }}
  {{- end }}
